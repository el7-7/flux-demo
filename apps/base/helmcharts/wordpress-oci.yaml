apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmChart
metadata:
  name: wordpress-oci
  namespace: flux-system
spec:
  chart: wordpress
  sourceRef:
    kind: OCIRepository
    name: bitnami-wordpress
    namespace: flux-system
  interval: 5m
  version: "30.0.3"
